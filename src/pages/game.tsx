import { GameBoard } from "@/features/game/game-board";
import { Container } from "@/shared/components/container";

export const Game = () => {
	return (
		<Container>
			<GameBoard />
		</Container>
	);
};
